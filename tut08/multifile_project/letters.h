#ifndef _LETTERS_H_
#define _LETTERS_H_
// Checks if the character passed in is a letter.
//
// `check_letter` will be passed in:
// - `ch` -- the character to check
//
// `check_letter` will return 1 if `ch` is a letter and 0 otherwise.
int check_letter(char ch);
#endif
